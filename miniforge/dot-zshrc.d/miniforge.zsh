# File content is based on what is automatically created by the conda init command
# It is needed so the conda and what is installed by it are available in the shell
if [[ -x /opt/homebrew/Caskroom/miniforge/base/bin/conda ]]; then
  eval "$(/opt/homebrew/Caskroom/miniforge/base/bin/conda shell.zsh hook)"
fi
